name: admin_website
description: E-Waste Admin Panel Web Application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
    
  # Supabase
  supabase_flutter: ^2.5.5
    
  # HTTP Requests
  http: ^1.1.0
  
  # Charts
  fl_chart: ^0.65.0
  
  # Icons
  cupertino_icons: ^1.0.6
  
  # Date formatting
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

